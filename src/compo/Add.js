import React from 'react'
import styled from 'styled-components'

const Nim = styled.h1`
    color: ${(props) => props.theme.fontColor};
`

function Add() {
    return (
        <Nim>
            Nimraaaaaaaaa
        </Nim>
    )
}

export default Add
